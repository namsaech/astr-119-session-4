import numpy as np
import matplotlib.pyplot as plt
import sys #gives access to a C-like sys library 
import os #gives access to an OS, functionality for any os , os module
print(sys.argv) #prints any command line argument
print(os.getcwd()) #prints current working directory